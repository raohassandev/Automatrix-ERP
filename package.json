{
  "private": true,
  "name": "automatrix-erp",
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "lint": "pnpm -C apps/web lint",
    "test": "pnpm -C apps/web test || true",
    "typecheck": "pnpm -C apps/web typecheck || pnpm -C apps/web tsc -p tsconfig.json --noEmit || true"
  }
}